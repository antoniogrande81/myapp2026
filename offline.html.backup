<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offline - MyApp</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#2400C1',
                        secondary: '#8E008C',
                        accent: '#00BFFF',
                        textDark: '#1C1C1E',
                        backgroundLight: '#F4F4F8',
                    }
                }
            }
        }
    </script>
    <style>
        body {
            background: linear-gradient(135deg, #2400C1, #8E008C);
            min-height: 100vh;
            font-family: 'Segoe UI', Roboto, sans-serif;
        }
        
        .text-white {
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.5);
        }
        
        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .offline-icon {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        .retry-button {
            transition: all 0.3s ease;
        }
        
        .retry-button:hover {
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="w-full px-4 py-4 relative z-10">
        <div class="max-w-md mx-auto flex items-center justify-center">
            <h1 class="text-xl font-bold text-white drop-shadow-lg">MyApp</h1>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 flex items-center justify-center px-4 pb-20">
        <div class="glass-effect rounded-2xl p-8 text-center max-w-sm w-full">
            
            <!-- Offline Icon -->
            <div class="offline-icon w-20 h-20 bg-gray-200 rounded-full mx-auto mb-6 flex items-center justify-center">
                <svg class="w-10 h-10 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-12.728 12.728m0-12.728l12.728 12.728M12 2C6.477 2 2 6.477 2 12s4.477 10 10 10 10-4.477 10-10S17.523 2 12 2z"></path>
                </svg>
            </div>
            
            <!-- Title -->
            <h2 class="text-2xl font-bold text-primary mb-4">
                Connessione Assente
            </h2>
            
            <!-- Message -->
            <p class="text-gray-600 mb-6 leading-relaxed">
                Non è possibile connettersi ai server di MyApp. Verifica la tua connessione internet e riprova.
            </p>
            
            <!-- Connection Status -->
            <div id="connectionStatus" class="mb-6 p-4 bg-red-50 rounded-lg border border-red-200">
                <div class="flex items-center justify-center space-x-2 text-red-700">
                    <div class="w-3 h-3 bg-red-500 rounded-full"></div>
                    <span class="text-sm font-medium">Offline</span>
                </div>
            </div>
            
            <!-- Available Offline Features -->
            <div class="mb-6 p-4 bg-blue-50 rounded-lg border border-blue-200">
                <h3 class="font-semibold text-blue-800 mb-3 text-sm">📱 Funzioni offline disponibili:</h3>
                <ul class="text-xs text-blue-700 space-y-1 text-left">
                    <li>• Visualizzazione tessera digitale salvata</li>
                    <li>• Consultazione convenzioni in cache</li>
                    <li>• Contatti dirigenti salvati</li>
                    <li>• Navigazione tra pagine visitate</li>
                </ul>
            </div>
            
            <!-- Action Buttons -->
            <div class="space-y-3">
                <button 
                    id="retryButton"
                    class="retry-button w-full bg-primary hover:bg-secondary text-white font-semibold py-3 px-6 rounded-lg shadow-lg flex items-center justify-center space-x-2"
                    onclick="checkConnection()"
                >
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                    </svg>
                    <span id="retryText">Riprova</span>
                </button>
                
                <button 
                    class="w-full bg-gray-500 hover:bg-gray-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors"
                    onclick="goHome()"
                >
                    🏠 Torna alla Home Offline
                </button>
            </div>
            
            <!-- Tips -->
            <div class="mt-6 pt-6 border-t border-gray-200">
                <h3 class="font-semibold text-textDark mb-2 text-sm">💡 Suggerimenti:</h3>
                <ul class="text-xs text-gray-600 space-y-1">
                    <li>• Verifica di essere connesso al WiFi</li>
                    <li>• Controlla i dati mobili se attivi</li>
                    <li>• Riavvia l'app se il problema persiste</li>
                </ul>
            </div>
            
        </div>
    </main>

    <script>
        let isRetrying = false;
        
        // Check connection status
        function updateConnectionStatus() {
            const statusDiv = document.getElementById('connectionStatus');
            const isOnline = navigator.onLine;
            
            if (isOnline) {
                statusDiv.innerHTML = `
                    <div class="flex items-center justify-center space-x-2 text-green-700">
                        <div class="w-3 h-3 bg-green-500 rounded-full"></div>
                        <span class="text-sm font-medium">Online - Connessione ripristinata!</span>
                    </div>
                `;
                statusDiv.className = 'mb-6 p-4 bg-green-50 rounded-lg border border-green-200';
                
                // Auto-reload after connection is restored
                setTimeout(() => {
                    window.location.reload();
                }, 2000);
            } else {
                statusDiv.innerHTML = `
                    <div class="flex items-center justify-center space-x-2 text-red-700">
                        <div class="w-3 h-3 bg-red-500 rounded-full"></div>
                        <span class="text-sm font-medium">Offline</span>
                    </div>
                `;
                statusDiv.className = 'mb-6 p-4 bg-red-50 rounded-lg border border-red-200';
            }
        }
        
        // Check connection manually
        async function checkConnection() {
            if (isRetrying) return;
            
            isRetrying = true;
            const button = document.getElementById('retryButton');
            const text = document.getElementById('retryText');
            
            // Update button state
            button.disabled = true;
            button.classList.add('opacity-75');
            text.textContent = 'Controllo...';
            
            try {
                // Try to fetch a small resource to check connectivity
                const response = await fetch('/favicon.ico', { 
                    cache: 'no-cache',
                    mode: 'no-cors'
                });
                
                // If successful, reload the page
                window.location.reload();
                
            } catch (error) {
                // Still offline
                text.textContent = 'Ancora offline';
                setTimeout(() => {
                    text.textContent = 'Riprova';
                    button.disabled = false;
                    button.classList.remove('opacity-75');
                    isRetrying = false;
                }, 2000);
            }
        }
        
        // Go to home page (cached version)
        function goHome() {
            window.location.href = '/';
        }
        
        // Listen for online/offline events
        window.addEventListener('online', updateConnectionStatus);
        window.addEventListener('offline', updateConnectionStatus);
        
        // Initialize
        updateConnectionStatus();
        
        // Auto-check connection every 30 seconds
        setInterval(() => {
            if (!isRetrying) {
                updateConnectionStatus();
            }
        }, 30000);
        
        // Service Worker communication
        if ('serviceWorker' in navigator) {
            navigator.serviceWorker.ready.then(registration => {
                // Listen for SW messages
                navigator.serviceWorker.addEventListener('message', event => {
                    if (event.data?.type === 'CACHE_UPDATED') {
                        console.log('Cache updated, content available offline');
                    }
                });
            });
        }
    </script>
</body>
</html>